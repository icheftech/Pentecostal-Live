fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic-settings==2.7.1
pytest==8.3.4
httpx==0.28.1
# Also install the command-builder package from the monorepo:
#   pip install ../../services/ffmpeg          (local dev, from apps/media-server)
#   pip install ./services/ffmpeg              (from repo root / Docker build)
